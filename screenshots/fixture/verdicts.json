{
  "modules_have_doc_comments": true,
  "errors_are_contextualized": {
    "relevant": false,
    "rationale": "the change only renames a struct field; no error handling touched"
  },
  "handlers_delegate_to_services": {
    "holds": false,
    "rationale": "user creation logic lives in the handler, not a service",
    "violations": [
      {
        "file": "src/users.rs",
        "line": 7,
        "message": "user creation logic lives in the handler, not a service"
      }
    ]
  }
}
