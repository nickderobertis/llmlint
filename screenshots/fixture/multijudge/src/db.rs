// database access layer
//
// Fixture source for the multi-judge screenshot scene: its contents do not
// matter (the verdicts are scripted), only that the `src/**` glob matches a file
// so the rule is evaluated rather than skipped.

pub fn load_user() {}
