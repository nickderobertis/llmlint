// user API handlers
//
// Fixture source for the screenshot capture: its contents do not matter (the
// verdicts are scripted), only that the `src/**` glob matches a file so the
// rules are evaluated rather than skipped.

pub fn create_user() {}
